# CLAUDE.md

このリポジトリは、Obsidian で運用している個人用 Vault（セカンドブレイン）です。
Claude がこの Vault 内で作業する際は、以下のルールに従ってください。

## リポジトリの目的

- 知識・アイデア・タスク・記録を蓄積し、後から検索・再利用するための個人ナレッジベースです。
- ソフトウェアプロジェクトではないため、ビルドやテストは存在しません。中心となる作業は
  「ノートの作成・編集・整理・リンク付け」です。

## フォルダ構成

Vault は PARA 方式をベースに整理します。フォルダが存在しない場合は、ノートの性質に応じて
新規作成して構いません。

- `0_Inbox/` — 未整理の一時メモ。とりあえず書き留める場所。定期的に他フォルダへ振り分ける。
- `1_Projects/` — 明確な終わりがあるプロジェクトに関するノート。
- `2_Areas/` — 継続的に関心を持ち続ける領域（健康、仕事、学習など）。
- `3_Resources/` — 特定のプロジェクトに紐づかない参考情報・調べ物。
- `4_Archive/` — 完了・非アクティブになった上記フォルダの内容。
- `Daily/` — デイリーノート（日付ベースの記録）。

既存ファイル（例: `my_notes`）を勝手に移動・リネームせず、ユーザーに確認してから整理する。

## ノート作成のルール

- ファイル名は内容が分かる日本語または英語のタイトルにする（例: `セカンドブレインの運用方針.md`）。
  日付ベースのノート（デイリーノート等）は `YYYY-MM-DD.md` 形式にする。
- 新規ノートには可能な限り frontmatter を付与する:

  ```yaml
  ---
  created: YYYY-MM-DD
  tags: []
  aliases: []
  ---
  ```

- 本文は Markdown で記述し、関連ノートへのリンクには Obsidian の Wikilink 記法 `[[ノート名]]` を使う。
- タグは軽量な分類目的で使用し、階層タグ（例: `#project/xxx`）も活用してよい。乱立を避け、
  既存タグを流用できないかまず確認する。

## Claude が作業する際の方針

- **ユーザーの言葉を尊重する**: ノートの要約・整理を行う際も、元の表現やニュアンスを勝手に
  変えず、加筆する場合は追記であることが分かるようにする。
- **事実を捏造しない**: ノートの内容について推測で情報を補わない。不明な点は確認する。
- **破壊的な変更は事前確認する**: 既存ノートの削除・大幅な書き換え・フォルダ構成の変更など、
  元に戻しにくい操作を行う前には、必ずユーザーに確認する。
- **リンクを意識する**: 新しいノートを作る際は、関連する既存ノートがないか探し、あれば
  `[[Wikilink]]` で接続することでナレッジグラフを育てる。
- **過剰な自動整理をしない**: 依頼されていない範囲まで勝手にフォルダ移動・タグ付け直しを
  しない。整理を提案したい場合は、まず提案し、実行前に許可を得る。
- 言語は基本的に日本語で応答・記述する。ユーザーが英語で書いたノートはそのまま尊重する。

## Git 運用

- コミットは明示的に依頼された場合のみ行う。
- Vault は個人データを含むため、機密性の高い内容をコミットする前には内容を確認する。
